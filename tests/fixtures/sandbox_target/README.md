# sandbox_target

Minimal fixture workspace for the PROMETHEUS sandbox enforcement suite and the
VibeThinker real-inference smoke test. PROMETHEUS reads/writes inside this
directory; the suite proves it cannot escape it.
