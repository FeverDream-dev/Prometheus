Step 1 · Welcome
  Welcome to PROMETHEUS
  
  A local-first, hardware-honest coding agent.
  This wizard picks a model bundle that fits your machine,
  checks Ollama, and gets you vibe-coding in under a minute.
  
  Next: confirm your project folder.

Step 2 · Project folder
  Project folder
  
  Path /tmp
  Git  clean
  
  PROMETHEUS will work in this folder.
  You can change it with: prometheus tui --workspace <path>
  
  Next: detect your hardware.

Step 3 · Hardware scan
  OS      Linux x86_64
  CPU     Demo CPU (8 cores)
  RAM     32 GB
  GPU     Demo GPU (mock)
  VRAM    8 GB
  Disk    180 GB free

Step 4 · Ollama status
  Ollama
    state:    running
    models:   3 installed
  
  If not running, start it with:
    ollama serve
  
  Next: pick a bundle.

Step 5 · Bundle recommendation
  Recommended bundle
  
    ember-8gb
  
  Based on your detected hardware. You can confirm,
  or browse alternatives in the next step.

Step 6 · Bundle choice
  Available bundles
  
    spark-cpu-8gb          Spark — CPU / 8 GB
    ember-8gb-gpu          Ember — 8 GB GPU
    forge-12gb             Forge — 12 GB balanced
    oracle-gemma4-12gb     Oracle — Gemma 4 multimodal
    titan-24gb             Titan — 24 GB quality
    hephaestus-code-24gb   Hephaestus — 24 GB coding team
    vibethinker-review-addon VibeThinker — optional review add-on (add-on)
    cloud-hybrid           Cloud-Hybrid — local-first with optional cloud fallback
  
  Type /use <id> to select one (in the main input).

Step 7 · Model pull / validate
  Models to pull
  
    qwen3.5:4b  (controller, ~3.4 GB, check_upstream)  installed required
    granite4.1:3b  (reviewer, ~2.1 GB, apache-2.0)  installed optional
  
  Confirmation required before each download (unless --yes).

Step 8 · Git & memory setup
  Git & memory setup
  
  Git
    clean
    branch: main
  
  Memory
    ready · 412/1024w
    words: 412/1024
  
  PROMETHEUS checkpoints every mutation via Git.
  Project memory lives in .prometheus/memory.md (bounded to 1024 words).

Step 9 · Ready to code
  Ready to code
  
  Type an objective and press Enter:
    add JWT auth to /api/login with tests
  
  Or open the command palette with Ctrl+P.

Interactive wizard: SetupWizard (pushed programmatically).
