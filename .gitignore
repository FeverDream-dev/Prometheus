.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
dist/
build/
*.egg-info/
.prometheus/
**/.prometheus/
*.log
.env


# PROMETHEUS hygiene (completion §9): never commit models, secrets, artifacts, or local state.
*.gguf
*.safetensors
*.bin
*.pt
*.ckpt
*.onnx
*.weights
config/*.env
*.env.*
!.env.example
**/.env
*.sqlite
*.sqlite3
*.db
prometheus.db*
.local-test/
.prometheus-test/
.artifacts/
.traces/
.recordings/
*.trace
*.har
*.webm
*.gif
*.mp4
playwright-report/
test-results/
.browser-data/
PROMETHEUS-Completion-Kit/

# local spec archives / duplicate extracts (provenance kept as .md only)
*.zip
PROMETHEUS-Completion-Kit*

# VibeThinker sandbox hygiene: caches + runtime state never committed.
models/
.ollama/
.cache/huggingface/
sandbox-test-report.json

# Local export artifacts (not project config).
exported-bundle.yaml

# AssetForge: generated asset binaries never committed (manifests + READMEs are OK).
assets/generated/**/source.png
assets/generated/**/transparent.png
assets/generated/**/web-*.png
assets/generated/**/preview.webp
.u2net/

# TUI visual smoke artifacts (regenerated by scripts/tui_visual_smoke.sh; never committed).
artifacts/tui/
